# Changelog V55

Simplicity and launch-readiness polish.

- Added `404.html` with clear Start and Continue options.
- Added `CNAME` for `willchecklist.com` so GitHub Pages can use the custom domain later.
- Added `.nojekyll` so GitHub Pages serves static files without Jekyll processing.
- Changed the top navigation Guide link from a dark button to a normal navigation link so it does not look like the primary action.
- Strengthened the top safe-notes notice contrast.
- Kept the six-step user flow unchanged.
