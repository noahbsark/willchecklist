# V79 — GitHub Pages Default URL Configuration

- Removed the `CNAME` file so GitHub Pages will use the default project URL for now.
- Updated canonical URLs, Open Graph URLs, Twitter image URLs, JSON-LD URLs, `robots.txt`, and `sitemap.xml` from `https://willchecklist.com/` to `https://noahbsark.github.io/willchecklist/`.
- Kept all user-facing six-step checklist flows unchanged.
- Kept `.nojekyll` for GitHub Pages compatibility.

Default public URL after GitHub Pages is enabled:

`https://noahbsark.github.io/willchecklist/`

When the custom domain is purchased later, add a `CNAME` file and update the site URLs back to the domain.
